<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:playround="http://schemas.android.com/apk/res-auto" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_upload_venue_add"
        android:icon="@drawable/ome_invite_add"
        android:title="@string/action_uploadvenue_add"
        android:state_pressed="true"
        android:drawable="@color/red"
        playround:showAsAction="ifRoom"  />

</menu>
