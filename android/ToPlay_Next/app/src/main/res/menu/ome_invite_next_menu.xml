<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:playround="http://schemas.android.com/apk/res-auto" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_invite_share"
        android:icon="@drawable/ic_menu_share"
        android:title="@string/action_invite_share"
        playround:showAsAction="ifRoom"  />
    <item android:id="@+id/action_invite_add"
        android:icon="@drawable/ome_invite_add"
        android:title="@string/action_invite_add"
        android:state_pressed="true"
        android:drawable="@color/red"
        playround:showAsAction="ifRoom"  />

</menu>
