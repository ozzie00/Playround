<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true" >
        <shape>
            <gradient
                android:endColor="@color/playround_default"
                android:startColor="@color/playround_default" />
            <size
                android:width="80dp"
                android:height="80dp" />
            <stroke
                android:width="1dp"
                android:color="@color/playround_default" />
            <corners
                android:radius="64dp" />
        </shape>
    </item>

    <item>
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="oval">
            <gradient
                    android:endColor="@color/white_absolute"
                    android:startColor="@color/white_absolute" />
            <size
                android:width="80dp"
                android:height="80dp" />
            <stroke
                    android:width="1dp"
                    android:color="@color/playround_default" />
            <corners
                    android:radius="64dp" />
        </shape>
    </item>

</selector>